<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ict.tools.ict_project_tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UnitConvertor_Activity" />
        <activity android:name=".length" />
        <activity android:name=".area" />
        <activity android:name=".calc" />
        <activity android:name=".fdcw" />
        <activity android:name=".EightQueen" />
        <activity android:name=".SolutionActivity" />
        <activity android:name=".volume" />
        <activity android:name=".weight"></activity>
    </application>

</manifest>
